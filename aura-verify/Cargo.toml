[package]
name = "aura-verify"
edition.workspace = true
version.workspace = true

[dependencies]
aura-ast = { path = "../aura-ast" }
aura-bridge = { path = "../aura-bridge" }
aura-ir = { path = "../aura-ir" }
aura-parse = { path = "../aura-parse" }
aura-nexus = { path = "../aura-nexus", default-features = false }
miette = { workspace = true }
thiserror = { workspace = true }

# Real SMT solver (statically linked via static-link-z3, requires C/C++/CMake build tools)
z3 = { workspace = true, optional = true }

[features]
default = []
z3 = ["dep:z3", "aura-nexus/z3"]

[dev-dependencies]

