[env]
# z3-sys 0.8.x vendors an older Z3 CMake project.
# CMake 4 removed compatibility policies older than 3.5.
# Keep the dependency build reproducible on modern CMake without requiring
# developers to configure this environment variable manually.
CMAKE_POLICY_VERSION_MINIMUM = "3.5"
