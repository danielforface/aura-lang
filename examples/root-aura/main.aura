# main.aura
# Root example entry: keep it lightweight and AVM-compatible.

import aura::lumina

cell main():
    val io = "io"
    io.println("Aura: main example")

    layout:
        VStack(padding: 18, spacing: 12, alignment: "center") {
            render: Text(text: "Aura Main", color: "Gold", size: 44)
            render: Text(text: "All examples runnable via tools/run_examples.py", color: "White", size: 18)
        }
