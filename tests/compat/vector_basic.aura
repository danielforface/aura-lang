import std::collections
import std::io

cell main() ->:
  val mut v: Tensor = collections::vector_new(4)
  collections::vector_set(v, 0, 7)
  val a: u32 = collections::vector_get(v, 0)
  val b: u32 = collections::vector_len(v)
  io::println("ok")
