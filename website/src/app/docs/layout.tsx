import Link from "next/link";
import { DOC_GROUPS } from "@/lib/siteData";
export default function DocsLayout({children}:{children:React.ReactNode}){return <div className="docs-shell"><aside className="docs-sidebar"><div className="docs-sidebar-title">Aura documentation</div>{DOC_GROUPS.map(group=><div className="docs-group" key={group.label}><div className="docs-group-label">{group.label}</div>{group.items.map(([slug,title])=><Link key={slug} href={`/docs/${slug}`}>{title}</Link>)}</div>)}</aside><article className="docs-article">{children}</article><aside className="docs-rail"><div className="docs-rail-card"><strong>Documentation truth rule</strong>The current compiler implementation is authoritative. The compact SDK reference is the next public semantic snapshot. Historical roadmap text can describe intent, but it does not override executable behavior.</div></aside></div>}
