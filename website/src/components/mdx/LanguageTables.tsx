const keywords = ["val", "mut", "cell", "extern", "trusted", "requires", "ensures", "assert", "assume", "if", "match", "while", "invariant", "decreases", "unsafe", "layout", "render"];
const operators = [["->", "synchronous flow / return syntax context"], ["~>", "asynchronous flow"], ["=", "binding / assignment context"], ["::", "module qualification"]] as const;
export function KeywordTable(){return <table><thead><tr><th>Keyword</th><th>Current role</th></tr></thead><tbody>{keywords.map(k=><tr key={k}><td><code>{k}</code></td><td>Current/reference-backed Aura language vocabulary.</td></tr>)}</tbody></table>}
export function OperatorTable(){return <table><thead><tr><th>Operator</th><th>Role</th></tr></thead><tbody>{operators.map(([op,role])=><tr key={op}><td><code>{op}</code></td><td>{role}</td></tr>)}</tbody></table>}
