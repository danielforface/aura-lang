# Enable AndroidX support (required by androidx.* and Material dependencies)
android.useAndroidX=true
# Migrate/support legacy support libraries if any transitive deps require it
android.enableJetifier=true
