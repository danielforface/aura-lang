export const metadata = { title: "Language explorer", description: "Interactive Aura syntax and proof-semantics documentation explorer; explicitly not a simulated browser compiler." };
export default function PlaygroundLayout({children}:{children:React.ReactNode}){return children}
